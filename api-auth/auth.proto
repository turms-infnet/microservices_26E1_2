syntax = "proto3";

package auth;

service GrpcAuthService {
  rpc verify_token(VerifyTokenRequest) returns (UserResponse);
}

message VerifyTokenRequest {
    string token = 1;
}

message UserResponse {
    int32 id = 1;
    string username = 2;
    string email = 3;
    bool is_valid = 4;
}